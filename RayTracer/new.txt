#include "publics.h"
#include "files.h"
#include "matrix.h"

#include "generateCubes.h"


int main() {

		// Declare memory block of size M
    std::string** a = new std::string*[iWidth];

    for (int i = 0; i < iWidth; i++) {
        a[i] = new std::string[iHeight]; // Declare a memory block of size n
    }

    // Traverse the 2D array
    for (int i = 0; i < iWidth; i++) {
        for (int j = 0; j < iHeight; j++) {
            a[i][j] = getRand();
        }
    }

	int x = 5, y = 7;

    for (int i = 0; i < iWidth; i++) {
        for (int j = 0; j < iHeight; j++) {
			if(i==x && j==y)
            a[i][j] = "0 0 0  ";
			}
        std::cout << std::endl;
    }

	x = 3, y = 4;

	 for (int i = 0; i < iWidth; i++) {
        for (int j = 0; j < iHeight; j++) {
			if(i==x && j==y)
            a[i][j] = "- - -  ";
			}
        std::cout << std::endl;
    }


	  // Traverse the 2D array
    for (int i = 0; i < iWidth; i++) {
        for (int j = 0; j < iHeight; j++) {
            std::cout << a[i][j] << " ";
        }
        std::cout << std::endl;
    }

    // Delete the array created
    for (int i = 0; i < iWidth; i++) // To delete the inner
                                // arrays
        delete[] a[i];
    delete[] a; // To delete the outer array which contained the pointers of all the inner arrays


	openFile();

	//Matrix *ptr;
	//ptr = new Matrix(iWidth,iHeight,"- - -\t");
	//ppmFS << *ptr;

	
	closeFile();

	return 0;
}
